{
  "devDependencies": {
    "@types/jquery": "^3.5.14",
    "@types/select2": "^4.0.55",
    "prettier": "^2.8.3",
    "rome": "^10.0.1",
    "typescript": "^4.7.2"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "esbuild": "^0.19.2"
  },
  "scripts": {
    "build": "esbuild library/javascript/*.* --bundle --sourcemap --minify --outdir=library/static --public-path=.",
    "watch": "npm run build -- --watch"
  }
}
